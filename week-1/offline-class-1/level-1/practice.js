// str.length used to find the length
function clg(data){
console.log(data);
}

// function lengthOf(str){
//     clg(str.length);
// }
// lengthOf("Heelo");

// function findIndexOf(str,target){
//     clg(str.indexOf(target));
// }
// findIndexOf("H l el o", " ");

function getSlice(str,start,end){
    clg(str.slice(start,end));
}
// getSlice("Slice",0,-1);
getSlice("Hello World",0,0);
getSlice("Hello World",0,-1);










